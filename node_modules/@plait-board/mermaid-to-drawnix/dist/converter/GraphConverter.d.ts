import { DrawnixConfig } from "../index.js";
import { MermaidToDrawnixResult } from "../interfaces.js";
import { Flowchart } from "../parser/flowchart.js";
import { Sequence } from "../parser/sequence.js";
export declare class GraphConverter<T = Flowchart | Sequence> {
    private converter;
    constructor({ converter, }: {
        converter: (graph: T, config: Required<DrawnixConfig>) => MermaidToDrawnixResult;
    });
    convert: (graph: T, config: DrawnixConfig) => MermaidToDrawnixResult;
}
