import { PointNode } from './graph';
export declare class PriorityQueue {
    list: {
        node: PointNode;
        priority: number;
    }[];
    constructor();
    enqueue(item: {
        node: PointNode;
        priority: number;
    }): void;
    dequeue(): {
        node: PointNode;
        priority: number;
    } | undefined;
}
