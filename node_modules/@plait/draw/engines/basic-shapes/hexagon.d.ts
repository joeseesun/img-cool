import { Point, RectangleClient } from '@plait/core';
import { ShapeEngine } from '../../interfaces';
export declare const getHexagonPoints: (rectangle: RectangleClient) => Point[];
export declare const HexagonEngine: ShapeEngine;
